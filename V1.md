# HSK Deck — v1 change list

Baseline: **v0** (tag `v0`, commit after App Store prep)  
Target: **v1**  
Status: implemented — automated tests green; manual iPhone checks remaining

---

## Summary (1–3 sentences)

> Fix polish bugs from feedback and add essential study improvements: bigger characters, swipe navigation, consistent fonts, pronunciation audio, and an in-app App Store update prompt.

---

## New features

| # | Feature | Why / notes | Priority (must / nice) | Done? |
|---|---------|-------------|------------------------|-------|
| 1 | Pronunciation / sounds | Device TTS. Chinese speaker under characters (front); English speaker next to meaning (back). Tap only. | must | [x] automated / [ ] iPhone audio |
| 2 | In-app update prompt | Soft prompt when App Store version &gt; installed. **Update** opens Store; **Later** dismisses until a newer Store version. Uses iTunes lookup + `@capacitor/app` / `@capacitor/browser`. | must | [x] version-compare tests / [ ] confirm after 1.1 is live on Store |

### Proposed (discuss before building)

| # | Feature | Why / notes | Priority | Done? |
|---|---------|-------------|----------|-------|
| 3 | App Store review reminder | Pre-prompt every **10** unique “I know it well” words; Not now → again after +10; Not really → email madebycaseyz@gmail.com; Yes → native review | must | [x] logic tests / [ ] iPhone |

---

## Changes / improvements

| # | Change | What was wrong / missing in v0 | Done? |
|---|--------|--------------------------------|-------|
| 1 | Make hanzi / words bigger + centered | Bigger but not huge; side space ≥ one character; face fills card so text is vertically centered; speaker under characters | [x] automated / [ ] visual on phone |
| 2 | Replace left/right arrows with swipe | Swipe left = next, swipe right = previous; arrows removed | [x] automated / [ ] feel on phone |
| 3 | Bigger rate buttons | Easier to tap Review / Know | [x] / [ ] feel on phone |

---

## Bug fixes

| # | Bug | How to reproduce | Fixed? |
|---|-----|------------------|--------|
| 1 | Font flash on card front | Removed CDN LXGW; single PingFang/system Chinese stack | [x] automated / [ ] cold start on phone |

---

## Out of scope for v1

- Cold-launch black screen (~10s) — saw once in TestFlight; not reproducing on App Store install. Skip for v1.

---

## Review reminder plan (implemented)

### Approach
1. **Pre-prompt:** “Enjoying HSK Deck?” → **Yes** / **Not really** / **Not now**
2. **Yes** → native StoreKit review (plugin); don’t ask again
3. **Not really** → support email **madebycaseyz@gmail.com**; don’t ask to rate again
4. **Not now** → ask again after **10 more** unique known words

### When
| Rule | Decision |
|------|----------|
| “Learnt” | Unique words marked **I know it well** across all levels |
| First ask | Know-count reaches **10** |
| After Not now | Know-count +**10** since last prompt |
| After Yes / Not really | Stop prompts for this install |
| Conflict | Hidden while in-app update prompt is visible |


---

## Testing plan

**Automated:** `npm test` — green  
**Manual:** web + physical iPhone still required for ship gate

### How to test (manual)

- Web: `npm run dev`
- iPhone: `npm run cap:sync` then Xcode Run / TestFlight

### 1. Bigger words (hanzi)

**Success (manual)**

- Hanzi larger than v0, not huge; vertically centered in the card
- Empty space on each side ≥ one character wide
- Speaker under characters; nothing cut off

**Success (automated)** — passing (`src/v1-config.test.ts`)

### 2. Swipe instead of arrows

**Success (manual)**

- Swipe left → next; swipe right → previous
- No arrow buttons
- Swipe ≠ flip; tap flips
- Ends of deck: no crash

**Success (automated)** — passing (`StudySession.test.tsx`)

### 3. Single font

**Success (manual):** no typeface swap after ~2s on cold start  
**Success (automated)** — passing (no LXGW in HTML/CSS)

### 4. Audio buttons

**Success (manual):** hear Chinese + English on iPhone; speaker does not flip card  
**Success (automated)** — passing (`speak.test.ts`, `StudySession.test.tsx`)

### 5. In-app update prompt

**Success (manual)**

- On a build **older** than the live App Store version, prompt appears
- **Update** opens the App Store listing
- **Later** hides it until a newer Store version exists
- Does not appear when installed version ≥ Store version

**Success (automated)** — version compare unit tests (`checkStoreUpdate.test.ts`)

### 6. Regression

**Success (manual):** know/review decks + progress persist  
**Success (automated):** rating callbacks still fire

### 7. Ship gate

- [ ] Manual checks on a physical iPhone
- [x] `npm test` green
- [ ] Version 1.1 build uploaded / released

### Automated test setup

- Vitest + React Testing Library + jsdom
- `npm test` / `npm run test:watch`
- `src/speech/speak.test.ts`, `src/components/StudySession.test.tsx`, `src/v1-config.test.ts`, `src/update/checkStoreUpdate.test.ts`

---

## App Store “What’s New” (draft)

```
What's new in v1:

• Larger characters on flashcards
• Swipe to go to the next or previous card
• Consistent Chinese font (no mid-flash font swap)
• Tap to hear Chinese or English pronunciation
• Notifies you when an App Store update is available
• Occasional prompt to rate the app if it’s helping you
```

---

## Version numbers (when shipping)

| Place | v0 / current | v1 target |
|-------|--------------|-----------|
| App Store version (`MARKETING_VERSION`) | 1.0 → codebase **1.1** | 1.1 |
| Build (`CURRENT_PROJECT_VERSION`) | 1 → codebase **2** | bump each upload |
| Git tag | `v0` | `v1` when released |

---

## Notes

- Pronunciation: Web Speech API (`src/speech/speak.ts`)
- Update check: `src/update/checkStoreUpdate.ts` + `UpdatePrompt`
- Font: PingFang SC / system stack only (no CDN LXGW)
- Swipe: left = next, right = previous
- Size: `.flashcard-front` uses `clamp(5rem, 13vw, 6.25rem)` + `padding-inline: 1em`; face fills card for vertical centering
